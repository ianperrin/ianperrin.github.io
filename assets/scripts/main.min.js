const menuToggle=document.getElementById("navbarNavCollection");if(menuToggle){const e=menuToggle.querySelectorAll("li.nav-item:not(.dropdown)"),o=new bootstrap.Collapse(menuToggle,{toggle:!1});e.forEach((e=>{e.addEventListener("click",(()=>{menuToggle.classList.contains("show")&&o.toggle()}))}))}
